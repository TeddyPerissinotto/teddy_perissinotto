<div class="education-section">

    <h1>Formation</h1>

    {%  for education in educations %}

        <div class="education-thumbnail">

            <h2>{{ education.year }} - {{ education.diploma }}</h2>
            <h3>{{ education.school}} - {{ education.city }}</h3>

        </div>

    {% endfor%}
</div>